# --- experiment results: SEPARATE repo (results/.git) ---
results/

# --- large generated data: traces, scratch DBs, indices ---
work/

# --- run logs (large, transient) ---
logs/

# --- sibling code repos (own git repos; method + baselines) ---

# --- compiled artifacts ---
driver/spfresh_driver
*.o
*.so
*.a
__pycache__/
*.pyc

# --- documents: kept local, not in the benchmark repo (per user) ---
docs/

# --- ledger + conversation log: kept local (per user) ---
PROGRESS.md
chat_history.md

# stray SPFresh artifact
DeletedIDs.bin
*.bin
