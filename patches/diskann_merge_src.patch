diff --git a/CMakeLists.txt b/CMakeLists.txt
index 0388717..da1f17d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -61,7 +61,7 @@ else()
 endif()
 
 add_definitions(-DMKL_ILP64)
-include_directories(include ${INTEL_ROOT}/include ${MKL_ROOT}/include ${BOOST_ROOT})
+include_directories(include ${INTEL_ROOT}/include ${MKL_ROOT}/include ${BOOST_ROOT} /usr/include/mkl)
 
 
 #Main compiler/linker settings 
diff --git a/include/v2/merge_insert.h b/include/v2/merge_insert.h
index 2c42b4f..e9e8eab 100644
--- a/include/v2/merge_insert.h
+++ b/include/v2/merge_insert.h
@@ -40,7 +40,7 @@ namespace diskann {
         Parameters& parameters, size_t dim, const std::string mem_prefix,
         const std::string disk_prefix_in, const std::string disk_prefix_out,
         Distance<T>* dist, diskann::Metric disk_metric, bool single_file_index,
-        std::string working_folder);
+        std::string working_folder, size_t merge_th = MERGE_TH);
 
     DISKANN_DLLEXPORT ~MergeInsert();
 
diff --git a/src/v2/merge_insert.cpp b/src/v2/merge_insert.cpp
index 6341e7a..0f349b2 100644
--- a/src/v2/merge_insert.cpp
+++ b/src/v2/merge_insert.cpp
@@ -61,12 +61,12 @@ namespace diskann {
       Parameters& parameters, size_t dim, const std::string mem_prefix,
       const std::string disk_prefix_in, const std::string disk_prefix_out,
       Distance<T>* dist, diskann::Metric dist_metric, bool single_file_index,
-      std::string working_folder)
+      std::string working_folder, size_t merge_th)
       : _dim(dim), _dist_metric(dist_metric),  _active_0(true), _active_1(false), _active_del_0(true),
         _active_del_1(false), _clearing_index_0(false),
         _clearing_index_1(false), _switching_disk_prefixes(false),
         _check_switch_index(false), _check_switch_delete(false) {
-    _merge_th = MERGE_TH;
+    _merge_th = merge_th;
     _single_file_index = single_file_index;
     this->_dist_metric = dist_metric;
     _mem_index_0 = std::make_shared<diskann::Index<T, TagT>>(
diff --git a/tests/CMakeLists.txt b/tests/CMakeLists.txt
index 8e4e9b3..d55e35d 100644
--- a/tests/CMakeLists.txt
+++ b/tests/CMakeLists.txt
@@ -89,4 +89,7 @@ endif()
 
 add_executable(overall_performance overall_performance.cpp)
 	target_link_libraries(overall_performance ${PROJECT_NAME} aio)
+
+add_executable(bench_stream_merge bench_stream_merge.cpp)
+	target_link_libraries(bench_stream_merge ${PROJECT_NAME} aio -ltcmalloc)
 # formatter
